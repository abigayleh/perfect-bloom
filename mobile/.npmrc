# expo-router pulls react-dom@19.2.8, which peer-requires react@19.2.8, while the
# SDK 57 template pins react@19.2.3. The conflict is upstream and harmless at
# runtime (react-dom is web-only and this app is native), but strict peer
# resolution refuses to install anything new without this.
legacy-peer-deps=true
